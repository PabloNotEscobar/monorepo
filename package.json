{
  "name": "monorepo-module-federation",
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "start": "concurrently \"npm start --prefix services/host\" \"npm start --prefix services/admin\" \"npm start --prefix services/shop\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
